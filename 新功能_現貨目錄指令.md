# 新功能指令 — 展示櫃現貨目錄

> **使用方式**：把這份指令貼給 Claude Code（桌面版 Code 模式）。
> 建議第一句先讓它讀專案備忘：「請先讀 @PROJECT_MEMO.md，然後執行這份指令。」

---

你好，我要在 HAMONDE CAFE 庫存系統新增一個功能：**展示櫃現貨目錄**。

## 功能目的

店裡沒有實體咖啡豆目錄，需要一個「現貨價目表」頁面，可以下載成圖片給客人看。以展示櫃的現貨為主。

---

## 一、入口

在首頁新增一個「現貨目錄」按鈕。

- 放在進階功能區塊（跟歷史紀錄、消耗報表、盤點並列），或視覺最協調的位置
- 圖示建議用 `menu-2` 或 `list-details` 或 `coffee`
- 路徑 `/menu`，新頁面 `src/pages/MenuPage.tsx`

---

## 二、顯示哪些豆子

撈出符合以下條件的豆子：
- **展示櫃（display）目前有庫存（> 0 包）**
- 且 status = `selling`（販售中）
- 且未軟刪除（deleted_at IS NULL）

排序：**依半磅價格由低到高**

---

## 三、顯示內容（精緻菜單式）

### 整體版面

- 外層背景：米灰 `#ACB6BF`
- 中央菜單卡片：奶白 `#FBF8F2`，圓角，置中，最大寬度約 580px
- 菜單卡片內容由上到下：

### 3.1 標題區（置中）
- Logo 圓圈（磚紅底 `#AC6342`，咖啡杯圖示 `coffee`）
- 店名「HAMONDE CAFE」（深棕 `#3D2817`，字距加大）
- 副標「現貨咖啡豆」（中棕 `#8B7355`，字距加大）

### 3.2 裝飾分隔線
- 兩條短線中間一個芥末黃 `#DFCC60` 圓點

### 3.3 豆子列表（每筆項目）

每筆豆子顯示三行：

**第一行**：豆名（左，深棕）+ 虛線連接 + 半磅價格（右，磚紅 `#AC6342`，格式 `$550`）
- 用 flex 排版，豆名靠左、價格靠右，中間用 dotted 虛線填滿（菜單經典樣式）

**第二行**：產地 + 處理法（中棕 `#8B7355`，小字）
- 格式：`地圖圖示 國家 產區 · 處理法`
- **單品豆**（origins 長度 = 1）：例如 `衣索比亞 耶加雪菲 · 日曬`
- **配方豆**（origins 長度 ≥ 2）：把所有國家用頓號連接 + 處理法，例如 `衣索比亞、巴西 · 水洗`
  - 配方豆只列國家，不列產區（避免太長）
  - 配方豆一樣顯示處理法
- 處理法顯示邏輯：有 process_detail 顯示細節，否則顯示大類別中文（日曬/水洗/蜜處理/特殊處理）
- 地圖圖示用 `map-pin`

**第三行**：參考風味（淺棕 `#A8957C`，小字）
- 格式：`參考風味：柑橘、莓果、花香`
- 風味用頓號連接，全部顯示（不限數量）
- 前綴固定為「參考風味：」

### 3.4 底部區

- 裝飾分隔線（同 3.2）
- 第一行說明：「以上價格為半磅（約 227g）／ 現貨供應」（中棕，置中，小字）
- 第二行備註：「風味描述會因沖煮方式、水溫、研磨粗細與個人感受略有差異，僅供參考。」（淺棕，置中，更小字，可換行）

### 不顯示的內容（重要）

- ❌ 不顯示庫存數量（這是給客人看的，不能洩漏庫存）
- ❌ 不顯示掛耳、手沖、虹吸價格（只顯示半磅）
- ❌ 不顯示成本、烘豆日期、養豆狀態等內部資訊
- ❌ 不顯示日期

---

## 四、下載圖片功能

### 4.1 下載按鈕

頁面上放一個「下載圖片」按鈕（磚紅色，明顯位置，例如菜單卡片上方或頁面頂部）。

- 圖示用 `download`
- 按鈕文字：「下載圖片」

### 4.2 下載邏輯

點擊按鈕後，把**菜單卡片區塊**（不含下載按鈕本身、不含外層頁面其他元素）轉成 PNG 圖片下載。

技術實作：
- 使用 `html2canvas` 套件（請用 npm 安裝：`npm install html2canvas`）
- 從 cdnjs 載入或 npm import 皆可，建議 npm import 比較穩定
- 設定 `scale: 2`（2 倍解析度，圖片清晰）
- 截圖範圍只包含菜單卡片（給菜單卡片一個 ref，html2canvas 只截那個元素）
- 下載時按鈕等互動元素不應出現在圖片中（因為只截菜單卡片區塊，按鈕在卡片外，自然不會被截到）

### 4.3 檔名格式

下載的檔名要含日期：
```
HAMONDE現貨菜單_YYYYMMDD.png
```
例如：`HAMONDE現貨菜單_20260524.png`

用當天日期，格式 YYYYMMDD（用 date-fns 的 format 或原生 Date 處理）。

### 4.4 下載過程的細節

- 點下載後，可短暫顯示「圖片產生中...」的提示（因為 html2canvas 需要 1-2 秒）
- 產生完成自動觸發下載
- 若失敗，顯示錯誤提示

---

## 五、空狀態

如果展示櫃目前沒有任何有庫存的販售中豆子：
- 顯示「目前展示櫃沒有現貨豆子」的提示
- 下載按鈕可隱藏或 disabled

---

## 六、視覺規範

沿用專案既有 cafe 配色（見 PROJECT_MEMO.md）：
- `#ACB6BF` 米灰背景
- `#AC6342` 磚紅（價格、Logo、下載按鈕）
- `#DFCC60` 芥末黃（裝飾點）
- `#FBF8F2` 奶白（菜單卡片）
- `#3D2817` 深棕（豆名）
- `#8B7355` 中棕（產地處理法、價格說明）
- `#A8957C` 淺棕（風味、備註）— 這是新的輔助色，比中棕更淺
- `#C4B5A0` 虛線顏色
- `#D6C9B8` 裝飾線顏色

平板優先，下載按鈕至少 44×44px，介面文字繁體中文。

---

## 七、實作後告知

完成後請告訴使用者：
1. 已新增「現貨目錄」功能
2. 本機測試方式（npm run dev，到 /menu 看效果）
3. 測試下載圖片功能是否正常
4. 確認沒問題後，協助使用者 git commit + push 部署上線

記得：因為新增了 html2canvas 套件，package.json 會變動，要一起 commit。

---

## 開發守則

- 程式碼風格簡潔
- 嚴格遵守視覺規範
- 介面文字繁體中文
- 不加陰影、漸層
- 絕對不要把 .env 推到 GitHub

請開始實作。完成後告訴使用者測試。
