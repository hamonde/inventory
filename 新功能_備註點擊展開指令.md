# 新功能指令 — 備註顯示優化（點擊展開彈窗）

> **使用方式**：把這份指令貼給 Claude Code（桌面版 Code 模式）。
> 建議第一句先讓它讀專案備忘：「請先讀 @PROJECT_MEMO.md，然後執行這份指令。」

---

你好，我要在 HAMONDE CAFE 庫存系統優化**備註欄位的顯示方式**。

## 需求背景

進出庫紀錄的備註欄位有時候會很長（例如客人訂購資訊、特殊事項說明），但現在的顯示方式會讓畫面很亂或看不到完整。需要做到：

1. 列表中備註欄位**簡潔顯示**（不撐爆版面、不參差不齊）
2. 想看完整內容可以**點擊跳彈窗**查看

---

## 一、修改範圍

請套用到以下兩個地方：

### 1.1 歷史紀錄頁（`/history`）
表格中的「備註」欄位。

### 1.2 批次明細彈窗
「查詢豆子庫存」頁點豆名 → 跳出批次明細彈窗 → 該彈窗內列出的交易紀錄，如果有顯示備註欄位也要套用。

**不需要修改的地方：**
- 進出庫頁的暫存表格（編輯中的內容不受影響）
- 新增/修改豆子等其他頁面

---

## 二、列表中的備註顯示規則

### 2.1 顯示樣式

備註欄位在列表中顯示為**單行截斷 + 視覺提示**：

- **有備註的情況**：
  - 顯示前面一小段文字（單行截斷，超過用 `...`）
  - 文字用磚紅色 `#AC6342` + 底線 + 滑鼠 cursor 變手指
  - 旁邊可加一個小放大鏡圖示 `ti-search` 或眼睛圖示 `ti-eye`（提示可點擊查看）
  - 整個備註區域可點擊（不只文字）
- **沒備註的情況**：
  - 顯示「—」破折號（中棕灰色 `#8B7355`）
  - 不可點擊

### 2.2 截斷長度

備註欄位寬度建議固定為 200px 左右（依現有表格欄位調整），文字超過就用 `text-overflow: ellipsis` 截斷。

關鍵 CSS：
```css
.note-preview {
  max-width: 200px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  color: #AC6342;
  text-decoration: underline;
  cursor: pointer;
}
```

### 2.3 互動

- 滑鼠移上去 → cursor 變手指 + 文字顏色稍微加深（hover 效果）
- 平板上：點擊就跳彈窗（不需要 hover 效果，但 cursor 樣式還是要設）

---

## 三、彈窗設計

點擊備註後跳出一個小彈窗，顯示完整資訊。

### 3.1 彈窗版面

```
┌─────────────────────────────┐
│ 紀錄詳細                 ✕  │
├─────────────────────────────┤
│ 備註                         │
│ ─────────────────────────── │
│ 客人特別訂購，5/20 提貨。   │
│ 客人姓陳，電話 0912-345-678，│
│ 要求烘豆日期 5/15 後，因為  │
│ 要送禮給長輩。              │
│                              │
│ 紀錄資訊                     │
│ ─────────────────────────── │
│ 日期：2026/05/14             │
│ 類型：售出（-3 包）          │
│ 倉庫：展示櫃                 │
│ 豆名：耶加雪菲 G2            │
│ 烘豆日期：2026/05/01         │
│ 操作員工：admin              │
│                              │
│            [ 關閉 ]          │
└─────────────────────────────┘
```

### 3.2 彈窗內容（從上到下）

**標題**：「紀錄詳細」

**備註區塊**：
- 小標「備註」
- 完整備註文字（保留換行、空白）
- 用較寬鬆的 line-height（1.6-1.8），方便閱讀
- 文字顏色：深棕 `#3D2817`

**紀錄資訊區塊**：
- 小標「紀錄資訊」
- 該筆交易的關鍵欄位：
  - 日期（YYYY/MM/DD）
  - 類型 + 數量（例：「售出 -3 包」、「進庫 +5 包」）
  - 倉庫（依類型顯示來源/目的）
    - 進庫：顯示 warehouse_to（目的倉庫）
    - 出庫/售出：顯示 warehouse_from（來源倉庫）
    - 上架：顯示「二樓倉庫 → 展示櫃」
    - 回倉：顯示「展示櫃 → 二樓倉庫」
  - 豆名（如果該豆已軟刪除，後綴「（已刪除）」）
  - 烘豆日期
  - 操作員工（顯示帳號前綴，去掉 @hamonde.local）
- 採用 label : value 的格式
- label 用中棕 `#8B7355`，value 用深棕 `#3D2817`

**底部按鈕**：
- 一個「關閉」按鈕（白底磚紅邊框，置中或靠右）

### 3.3 彈窗樣式

沿用專案既有設計：
- 白底圓角，圓角 8-12px
- 標題列下方有分隔線
- 區塊之間有適當間距
- 平板友善（彈窗最寬 480px 左右，內容置中）
- 點背景遮罩或右上 ✕ 可關閉

---

## 四、空備註處理

如果某筆紀錄的 note 欄位是 `null` 或空字串：
- 列表中顯示「—」破折號
- 該欄位不可點擊
- cursor 維持預設箭頭

---

## 五、實作建議

### 5.1 元件化

建議建立一個共用元件 `NoteCell`（或類似名稱），用在歷史紀錄頁和批次明細彈窗。

元件接收 props：
- `note`: string | null
- `transactionInfo`: 該筆紀錄的相關資訊（日期、類型、倉庫、豆名、烘豆日期、操作員工）

元件內部處理：
- 列表顯示（截斷或破折號）
- 點擊跳彈窗
- 彈窗內容渲染

這樣兩個地方都能複用。

### 5.2 彈窗共用

如果專案已有現成的 Dialog/Modal 元件（例如 shadcn/ui 的 Dialog），請直接複用。

---

## 六、視覺規範

沿用 cafe 配色：
- 列表中可點擊備註：磚紅 `#AC6342` 文字 + 底線
- 列表中空備註：中棕 `#8B7355` 破折號
- 彈窗背景：奶白 `#FBF8F2` 或白色
- 彈窗標題：深棕 `#3D2817`
- 彈窗內 label：中棕 `#8B7355`
- 彈窗內 value：深棕 `#3D2817`
- 分隔線：米色 `#D6C9B8`

平板優先，所有可點擊區域最小 44×44px。

---

## 七、實作後告知

完成後請告訴使用者：
1. 已修改歷史紀錄頁和批次明細彈窗的備註顯示
2. 本機測試方式（npm run dev，到 /history 頁面或查詢豆子庫存 → 點豆名 看效果）
3. 測試重點：
   - 短備註是否正常顯示（不截斷）
   - 長備註是否正確截斷顯示「...」
   - 點擊備註是否跳出彈窗
   - 彈窗內容是否完整正確
   - 空備註是否顯示「—」且不可點擊
4. 確認沒問題後，協助使用者 git commit + push 部署上線

---

## 開發守則

- 程式碼風格簡潔
- 嚴格遵守視覺規範
- 介面文字繁體中文
- 不加陰影、漸層

請開始實作。完成後告訴使用者測試。
