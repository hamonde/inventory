# Phase 3 指令 — HAMONDE CAFE 庫存系統進階功能

> **使用方式**：把下方所有內容貼給 Claude Code（桌面版 Code 模式），完成 Phase 3。  
> Phase 1、Phase 2 已完成，這是最後一個階段。

---

你好，這是 HAMONDE CAFE 咖啡豆庫存系統的 **Phase 3 開發指令**。Phase 1 和 Phase 2 都已完成，現在做進階功能。

## 一、Phase 3 任務總覽

依優先順序完成以下 3 項：

1. **快速售出 / 上架捷徑**（最常用，先做）
2. **每日消耗報表**
3. **盤點功能**

**請依序完成，每完成一項就回報，等使用者測試後再繼續下一項。**

---

## 二、視覺規範（沿用前兩階段）

繼續使用既有的 cafe 配色：
- 米灰背景 `#ACB6BF` / `cafe-bg`
- 磚紅主色 `#AC6342` / `cafe-primary`
- 芥末黃點綴 `#DFCC60` / `cafe-accent`
- 奶白卡片 `#FBF8F2` / `cafe-cream`
- 深棕主文字 `#3D2817` / `cafe-dark`
- 中棕次要文字 `#8B7355` / `cafe-muted`
- 米色邊框 `#D6C9B8` / `cafe-border`

平板優先設計，所有按鈕最小 44×44px，圓角 8-12px，不用陰影。

---

## 三、任務 1：快速售出 / 上架捷徑

### 3.1 入口位置

在首頁，於主功能 4 大卡片上方或下方（請選擇視覺最協調的位置），新增「快速操作」區塊，包含兩個按鈕：

- **快速售出**（磚紅色大按鈕，圖示用 `coins` 或 `cash`）
- **快速上架**（芥末黃色或淺磚紅，圖示用 `package-import` 或 `arrow-up`）

按鈕設計：
- 大尺寸（適合平板手指快速點擊）
- 圖示 + 文字垂直排列
- 圓角 12px

### 3.2 快速售出彈窗

點擊「快速售出」開啟彈窗（不跳轉頁面）。

**版面**：
- 標題：「快速售出」+ 副標「展示櫃 → 客人」
- 預設值（系統自動填好，員工不用選）：
  - 類型：售出（`sell`）
  - 日期：今天
  - 倉庫：展示櫃（warehouse_from = `display`）
- 員工只需填的欄位：
  1. **豆名**：下拉選單（撈 status=selling 且未軟刪除的豆子，且該豆在展示櫃有庫存的）
  2. **烘豆日期**：下拉選單（撈展示櫃該豆現有的所有批次，格式「YYYY/MM/DD（剩 X 包）」，預設最舊）
  3. **半磅數量**：數字輸入（正整數，+/- 按鈕加數字鍵盤）
  4. **備註**（選填）：文字輸入
- 底部：取消 + 確認售出 兩個按鈕

**互動行為**：
- 點「確認售出」 → 寫入 inventory_transactions → 顯示「售出成功」toast → 關閉彈窗 → 回首頁
- 寫入時 operator_id = 當前登入者 uid
- 彈窗開啟時若該倉庫完全沒有任何豆子有庫存，顯示提示「展示櫃目前沒有可售出的豆子」並把欄位 disabled
- 若員工選的豆子沒有任何批次，提示「此豆在展示櫃無庫存」
- **倉庫切換**：彈窗中允許員工臨時改成「二樓倉庫」（小切換按鈕，預設展示櫃），以防偶爾從二樓售出

**驗證**：
- 數量不可超過該批次現有庫存
- 數量必須為正整數

### 3.3 快速上架彈窗

點擊「快速上架」開啟彈窗。

**版面**：
- 標題：「快速上架」+ 副標「二樓倉庫 → 展示櫃」
- 預設值：
  - 類型：上架（`shelf`）
  - 日期：今天
  - 倉庫來源：二樓倉庫（warehouse_from = `storage`）
  - 倉庫目的：展示櫃（warehouse_to = `display`）
- 員工填的欄位：
  1. **豆名**：下拉選單（撈 status=selling 且在二樓倉庫有庫存的）
  2. **烘豆日期**：下拉選單（撈二樓倉庫該豆現有所有批次，預設最舊）
  3. **半磅數量**：數字輸入
  4. **備註**（選填）
- 底部：取消 + 確認上架

**互動行為**：
- 點「確認上架」 → 寫入 inventory_transactions → 「上架成功」toast → 關閉彈窗
- 二樓沒有可上架的豆子時提示

**驗證**：同上

### 3.4 操作完成後的回饋

成功後在首頁右上角或彈窗關閉位置顯示 toast：
- 「✓ 已售出 3 包 衣索比亞耶加雪菲」
- 「✓ 已上架 5 包 巴西喜拉朵」

維持 2-3 秒後自動消失。

---

## 四、任務 2：每日消耗報表

### 4.1 入口

首頁加入「消耗報表」按鈕（位於 4 大主功能之外、新鮮度警示和歷史紀錄附近），路徑 `/reports`，新頁面 `src/pages/ReportsPage.tsx`。

### 4.2 頁面內容

**頁面標題**：「消耗報表」

**時間區間切換器**（頁面上方）：

點選按鈕切換時間區間：
- 本月（預設）
- 上月
- 最近 7 天
- 自選區間（彈出兩個日曆讓使用者選起始日和結束日）

**主表格欄位**（依消耗量由高到低排序）：

| 欄位 | 計算邏輯 |
|------|---------|
| 豆名 | 豆子名稱 |
| 售出量（包） | 該區間內 transaction_type='sell' 的 qty_half_pound 加總 |
| 出庫量（包） | 該區間內 transaction_type='out' 的 qty_half_pound 加總 |
| 總消耗（包） | 售出量 + 出庫量 |
| 售出金額（NT$） | 售出量 × beans.price_half_pound |

**注意**：
- 不計算 `in`、`shelf`、`return` 類型（這些是內部移動或進貨，不是消耗）
- 只列有消耗的豆子（售出量+出庫量 > 0）
- 已軟刪除的豆子若該區間有消耗紀錄，也要顯示，後綴顯示「（已刪除）」

**表格底部**：總計列
- 顯示所有豆子的售出量合計、出庫量合計、總消耗合計、售出金額合計

**互動**：
- 點欄位標題可切換排序方向（升序/降序）
- 預設按「總消耗」由高到低
- 點豆名 → 彈出該豆子的明細卡片（產地、處理法、價格等，類似查詢豆子庫存頁的卡片）

**空狀態**：該區間無消耗時顯示「此區間沒有消耗紀錄」

### 4.3 視覺風格

- 表格樣式跟「查詢豆子品項」一致
- 「售出金額」欄位靠右對齊，數字用 toLocaleString() 加千分位（例如 NT$ 24,500）
- 總計列用 `cafe-cream` 背景區隔

---

## 五、任務 3：盤點功能

### 5.1 入口

首頁加入「盤點」按鈕（跟消耗報表並列），路徑 `/inventory-check`，新頁面 `src/pages/InventoryCheckPage.tsx`。

### 5.2 盤點流程設計

**第一步：選擇要盤點的倉庫**

進入頁面後，先讓員工選擇要盤點的倉庫：
- 二樓倉庫
- 展示櫃

選擇後進入第二步。

**第二步：列出所有需要盤點的批次**

撈出該倉庫**目前有庫存的所有「豆子+批次」組合**，列表式呈現：

| 欄位 | 顯示 |
|------|------|
| 豆名 | 文字 |
| 烘豆日期 | YYYY/MM/DD |
| 狀態徽章 | 養豆中 / 正常 / 過期警示（沿用顏色標示） |
| 系統庫存 | 顯示目前系統算出的數量 |
| 實際清點 | 數字輸入欄（員工填寫） |
| 差異 | 自動計算（實際 - 系統），>0 顯示綠色 +N，<0 顯示紅色 -N，=0 顯示 — |

**第三步：員工依序輸入實際清點數量**

- 預設「實際清點」欄位為空（不要預填系統庫存，避免員工懶得數）
- 員工每填一筆，差異欄位即時計算
- 員工填完後可看到一個總覽：共幾筆差異、總差異多少包

**第四步：送出盤點**

底部「送出盤點調整」按鈕：
- 點擊後彈出二次確認，列出所有差異：
  ```
  即將為以下批次產生盤點調整紀錄：
  - 衣索比亞耶加雪菲（烘豆 4/28）：盤點 -2 包
  - 巴西喜拉朵（烘豆 5/10）：盤點 +1 包
  確定送出嗎？
  ```
- 確認後一次性寫入多筆 inventory_transactions：
  - 差異 > 0（多出來）→ transaction_type = `in`，數量 = 差異
  - 差異 < 0（少了）→ transaction_type = `out`，數量 = 差異絕對值
  - 差異 = 0 → 不寫入
  - 所有紀錄的 `note` 欄位填「盤點調整」
  - 倉庫欄位依差異類型：
    - in：warehouse_to = 員工選的倉庫
    - out：warehouse_from = 員工選的倉庫
  - operator_id = 當前登入者
- 寫入成功後顯示「盤點完成，共產生 N 筆調整紀錄」toast，回到第一步（讓員工繼續盤另一個倉庫）

### 5.3 視覺與互動細節

- 列表用表格呈現，每列高度足夠（平板手指好點）
- 「實際清點」欄位用大字輸入框，配 +/- 按鈕
- 「差異」欄位即時更新，顏色明顯
- 頂部有進度提示：「已盤點 5 / 12 批次」
- 中途想取消盤點：頂部「取消」按鈕，提示「未送出的資料會遺失」二次確認後返回

### 5.4 注意事項

- 盤點過程中可能會花很久（員工要去倉庫實際數），所以**頁面狀態要保留在 React state，不要因為刷新頁面而丟失**
- 提醒使用者：盤點過程中如果有其他人在同時做進出庫，可能導致差異不準（建議盤點時暫停其他操作）
- 軟刪除的豆子若還有庫存，也要列入盤點

---

## 六、首頁版面整理

Phase 3 完成後，首頁會有蠻多元素，請整理一下版面：

**建議版面結構**（從上到下）：

1. Header（既有）
2. 新鮮度警示卡片（Phase 2 既有）
3. **快速操作區塊**（新增）：快速售出、快速上架 兩個大按鈕
4. **主功能區塊**：4 大功能卡片（既有）
5. **進階功能區塊**（新增）：歷史紀錄、消耗報表、盤點 三個次要按鈕（並列、較小）

讓視覺有層次：
- 警示卡片：最重要、最上面
- 快速操作：每天用、放醒目位置
- 主功能：核心操作、佔最大空間
- 進階功能：偶爾用、放下方

---

## 七、實作優先順序

按以下順序：

1. **任務 1：快速售出 / 上架捷徑**（用得最多，先做）
2. **任務 2：每日消耗報表**
3. **任務 3：盤點功能**
4. **首頁版面整理**（最後做，全部完成後再整理）

每完成一個任務就回報，等使用者測試確認後再進下一個。

---

## 八、開發守則

- 程式碼風格簡潔，避免過度抽象
- 元件保持中等粒度
- 所有錯誤情況都要有 user-friendly 的提示（toast 或 inline message）
- 表單驗證在 client 端先做一輪
- 操作完成後給明確視覺回饋
- 嚴格遵守視覺規範（顏色、圓角、留白、按鈕大小）
- 不加陰影、漸層、毛玻璃效果
- 介面文字一律繁體中文

---

請開始實作任務 1（快速售出 / 上架捷徑）。完成後告訴使用者測試。
